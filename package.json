{
  "name": "rh-alpha-tracker",
  "version": "0.1.0",
  "description": "Early-entry token scanner and scoring dashboard for Robinhood Chain",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "scan": "node --env-file-if-exists=.env src/scanner.js",
    "discover": "node --env-file-if-exists=.env src/discover.js"
  },
  "dependencies": {
    "ethers": "^6.17.0",
    "express": "^4.21.1",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=20.6"
  }
}
